# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Entorno (las claves nunca al repo; .env.production SI se versiona,
# solo lleva la URL publica del backend)
.env
.env.local

# ============================================================
# Nativo. A diferencia de gonper-app —donde android/ entero esta
# ignorado y por eso la carpeta vive en un solo disco— aqui SI se
# versiona el proyecto Android. Lo unico que queda fuera son los
# secretos de firma y lo que genera el build.
# ============================================================
android/app/*.jks
android/app/*.keystore
android/*.jks
android/*.keystore
android/keystore.properties
android/local.properties
android/.gradle/
android/build/
android/app/build/
android/app/src/main/assets/public/
android/capacitor-cordova-android-plugins/

ios/App/Pods/
ios/App/App/public/
ios/App/build/
.ios-temp/
